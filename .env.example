# --- Server (Vercel / local dev API) — NEVER commit real keys ---
OPENAI_API_KEY=
# Optional override (default gpt-4o)
# OPENAI_MODEL=gpt-4o

# --- Frontend (Vite) — safe to expose ---
# Production: your deployed origin, e.g. https://your-app.vercel.app
# Leave empty during local dev; Vite proxies /api to the dev server.
# VITE_CRITIQUE_API_URL=

# Set to "true" to force local heuristic analysis (no API call)
# VITE_USE_LOCAL_CRITIQUE=
